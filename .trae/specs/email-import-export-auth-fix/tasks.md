# 邮件导入导出认证问题修复 - 实现计划

## [x] 任务 1：修复邮件导出功能的认证问题
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 修改前端 `handleExport` 函数，使用 `fetch` API 替代 `window.open`
  - 在请求头中添加认证 token
  - 处理响应并下载文件
- **验收标准**：AC-1
- **测试要求**：
  - `programmatic` TR-1.1：导出邮件时包含认证 token
  - `programmatic` TR-1.2：成功导出邮件而不提示认证失效
- **注意**：需要确保文件下载的正确处理

## [x] 任务 2：修复邮件导入功能的认证问题
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 修改前端 `handleImport` 函数，在 `fetch` 请求中添加认证 token
  - 确保认证 header 正确设置
- **验收标准**：AC-2
- **测试要求**：
  - `programmatic` TR-2.1：导入邮件时包含认证 token
  - `programmatic` TR-2.2：成功导入邮件而不提示认证失效
- **注意**：需要确保文件读取和请求发送的正确性

## [x] 任务 3：添加批量邮件导出功能
- **优先级**：P1
- **依赖**：任务 1
- **描述**：
  - 优化批量导出逻辑，使用 Promise.all 并行处理
  - 显示导出进度和状态
  - 确保所有选中邮件都能成功导出
- **验收标准**：AC-3
- **测试要求**：
  - `programmatic` TR-3.1：批量导出多个邮件
  - `programmatic` TR-3.2：显示导出进度
- **注意**：需要考虑性能和用户体验

## [x] 任务 4：添加批量邮件导入功能
- **优先级**：P1
- **依赖**：任务 2
- **描述**：
  - 修改文件输入元素，支持 multiple 属性
  - 实现批量文件读取和处理
  - 显示批量导入进度和状态
- **验收标准**：AC-4
- **测试要求**：
  - `programmatic` TR-4.1：批量导入多个 .eml 文件
  - `programmatic` TR-4.2：显示导入进度
- **注意**：需要考虑文件大小和处理时间

## [x] 任务 5：测试和验证
- **优先级**：P0
- **依赖**：任务 1, 2, 3, 4
- **描述**：
  - 测试单个邮件导入导出功能
  - 测试批量邮件导入导出功能
  - 验证认证问题是否完全解决
- **验收标准**：AC-1, AC-2, AC-3, AC-4
- **测试要求**：
  - `programmatic` TR-5.1：所有功能正常工作
  - `human-judgment` TR-5.2：用户体验良好
- **注意**：需要在不同浏览器环境下测试