# 增加用户多邮件签名功能实现计划

## [ ] 任务1：修改后端账户实体，添加多签名支持
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 修改 `mail-worker/src/entity/account.js`，将单个 signature 字段改为支持多个签名的结构
  - 可以使用 JSON 类型存储多个签名信息
  - 更新 `mail-worker/src/init/init.js`，添加数据库迁移逻辑
- **成功标准**：
  - 账户实体支持存储多个签名
  - 数据库迁移顺利完成
- **测试要求**：
  - `programmatic` TR-1.1: 账户实体结构正确，支持多签名存储
  - `programmatic` TR-1.2: 数据库迁移脚本执行成功
  - `programmatic` TR-1.3: init文件更新完成，数据库初始化正常

## [ ] 任务2：更新后端 API 接口，支持多签名管理
- **优先级**：P0
- **依赖**：任务1
- **描述**：
  - 修改 `account-api.js` 和 `setting-api.js`，添加多签名相关的接口
  - 支持签名的添加、编辑、删除、获取操作
- **成功标准**：
  - API 接口能够正确处理多签名的管理操作
  - 接口返回数据格式正确
- **测试要求**：
  - `programmatic` TR-2.1: 所有签名管理接口返回正确的状态码
  - `programmatic` TR-2.2: 接口能够正确处理签名的增删改查操作

## [ ] 任务3：更新后端服务层，处理多签名逻辑
- **优先级**：P0
- **依赖**：任务2
- **描述**：
  - 修改 `account-service.js` 和 `setting-service.js`，添加多签名相关的业务逻辑
  - 确保签名数据在账户操作时正确处理
- **成功标准**：
  - 服务层能够正确处理多签名的业务逻辑
  - 签名数据能够正确保存和加载
- **测试要求**：
  - `programmatic` TR-3.1: 服务层能够正确处理签名的增删改查
  - `programmatic` TR-3.2: 签名数据在账户操作时能够正确处理

## [ ] 任务4：修改前端账户设置界面，添加多签名管理功能
- **优先级**：P1
- **依赖**：任务3
- **描述**：
  - 修改 `mail-vue/src/views/account/index.vue`，在 SMTP 配置对话框中添加多签名管理功能
  - 提供签名的添加、编辑、删除、设置默认等操作
- **成功标准**：
  - 界面能够正确显示和管理多个签名
  - 用户操作流畅，体验良好
- **测试要求**：
  - `human-judgement` TR-4.1: 界面布局合理，操作流畅
  - `programmatic` TR-4.2: 签名管理操作能够正确调用 API

## [ ] 任务5：修改前端邮件编辑界面，添加签名选择功能
- **优先级**：P1
- **依赖**：任务4
- **描述**：
  - 修改 `mail-vue/src/layout/write/index.vue`，添加签名选择功能
  - 在邮件编辑界面显示签名选择器，允许用户选择要使用的签名
- **成功标准**：
  - 邮件编辑界面能够显示签名选择器
  - 用户能够方便地选择和切换签名
- **测试要求**：
  - `human-judgement` TR-5.1: 签名选择器界面美观，操作方便
  - `programmatic` TR-5.2: 签名选择能够正确应用到邮件内容

## [ ] 任务6：更新前端 API 调用，支持多签名操作
- **优先级**：P1
- **依赖**：任务3
- **描述**：
  - 修改 `mail-vue/src/request/setting.js`，添加多签名相关的 API 调用函数
  - 确保签名数据在前端能够正确传递和处理
- **成功标准**：
  - 前端能够正确调用后端的多签名管理接口
  - 签名数据能够正确传递和处理
- **测试要求**：
  - `programmatic` TR-6.1: API 调用函数能够正确处理多签名数据
  - `programmatic` TR-6.2: 前端能够正确处理 API 返回的数据

## [ ] 任务7：功能测试和优化
- **优先级**：P2
- **依赖**：任务5、任务6
- **描述**：
  - 测试多签名功能的完整流程
  - 优化界面和用户体验
  - 修复可能存在的问题
- **成功标准**：
  - 多签名功能能够正常工作
  - 用户体验良好
  - 没有明显的 bug
- **测试要求**：
  - `programmatic` TR-7.1: 所有功能测试通过
  - `human-judgement` TR-7.2: 界面美观，操作流畅

## 实现步骤

1. 首先修改后端账户实体，添加多签名支持
2. 更新后端 API 接口，支持多签名的管理操作
3. 更新后端服务层，处理多签名的业务逻辑
4. 修改前端账户设置界面，添加多签名管理功能
5. 修改前端邮件编辑界面，添加签名选择功能
6. 更新前端 API 调用，支持多签名操作
7. 测试所有功能是否正常工作
8. 优化界面和用户体验

## 预期效果

- 每个邮件账户可以设置多个签名
- 用户可以为每个签名设置名称和内容
- 用户可以设置默认签名
- 发送邮件时可以选择使用哪个签名
- 签名管理界面操作简单直观，用户体验良好