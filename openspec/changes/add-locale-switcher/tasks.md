## 1. 实现语言切换按钮

- [x] 1.1 在 `layout/header/index.vue` 模板的 `.toolbar` 区域，主题切换按钮（太阳/月亮图标）之前，添加语言切换按钮元素，使用 `.icon-item` 样式类
- [x] 1.2 按钮文字根据 `settingStore.lang` 动态显示："EN"（当前中文时）或 "中"（当前英文时），点击调用已有的 `changeLang()` 函数
- [x] 1.3 为语言切换按钮添加 `.lang-switch` 样式类，设置字体大小、字重等细节样式，确保与 `.icon-item` 视觉一致

## 2. 验证

- [x] 2.1 验证点击按钮可正确切换语言，按钮文字同步更新
- [x] 2.2 验证工具栏布局顺序正确：语言切换 → 主题切换 → 通知 → 用户头像
- [x] 2.3 验证移动端显示正常，不溢出
